1) conf : contains the confoguration files allwing to:
	a) set weights of criteria
	b) set thresholds
	c) set N for N-gram algorithm
2) models : contains a set of models for test
3) the class test.Test performs a simple test